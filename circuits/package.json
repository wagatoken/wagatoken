{
  "name": "waga-zk-circuits",
  "version": "1.0.0",
  "description": "ZK circuits for WAGA Coffee privacy and selective transparency",
  "main": "index.js",
  "scripts": {
    "build": "node build-circuits.js",
    "build:price": "circom2 PricePrivacyCircuit.circom --r1cs --wasm --sym --c",
    "build:quality": "circom2 QualityTierCircuit.circom --r1cs --wasm --sym --c",
    "build:supply": "circom2 SupplyChainPrivacyCircuit.circom --r1cs --wasm --sym --c",
    "clean": "rm -rf build artifacts",
    "test": "node test-circuits.js"
  },
  "keywords": ["zk-proofs", "circom", "privacy", "coffee", "blockchain"],
  "author": "WAGA Team",
  "license": "MIT",
  "dependencies": {
    "circom2": "^0.2.22",
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "chai": "^4.3.7"
  }
}
